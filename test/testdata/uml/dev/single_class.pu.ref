@startuml
    class "SingleClass" {
        +SingleClass()
        +SingleClass(const SingleClass &other)
        +SingleClass & operator=(const SingleClass &other) /* operator */
        +virtual ~SingleClass()
    }
@enduml
